{
  "name": "pr-review-extension",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["bridge"],
  "scripts": {
    "install-host": "node bridge/install-host.js",
    "test": "node --test 'bridge/test/**/*.test.js'"
  }
}
